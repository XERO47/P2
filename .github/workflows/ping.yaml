name: Ping URL

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch: # Add this manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the URL
        run: curl -sS https://your-url-to-ping.com > /dev/null