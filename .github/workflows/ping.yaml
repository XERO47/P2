name: Ping URL

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the URL
        run: curl -sS https://p2-y6wv.onrender.com/health/ > /dev/null